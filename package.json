{
  "name": "remix-intelligence-studio",
  "version": "0.1.0",
  "description": "Multi-agent executive signal-mix pipeline — React + Vite + Cloudflare Pages",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:local": "wrangler pages dev -- npm run dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "lucide-react": "^0.400.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "wrangler": "^3.90.0"
  },
  "engines": {
    "node": ">=20"
  }
}
